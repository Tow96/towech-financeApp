{
  "name": "@financeapp/source",
  "version": "3.0.0",
  "license": "BSD-3-Clause",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace webclient\" \"npm run dev --workspace backend\"",
    "format": "prettier --check ./apps",
    "format:fix": "prettier --write ./apps",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "private": true,
  "workspaces": [
    "./apps/*"
  ],
  "devDependencies": {
    "@types/node": "22.13.1",
    "concurrently": "^9.2.0",
    "drizzle-kit": "^0.31.1"
  }
}
