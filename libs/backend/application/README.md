# backend-application

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build backend-application` to build the library.

## Running unit tests

Run `nx test backend-application` to execute the unit tests via [Jest](https://jestjs.io).
